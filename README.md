# Intern Tracker
This is a CRUD application built .

## How to Run Locally
1. Clone this repo.
2. Open two terminals.
3. In Terminal 1 (Server): `cd server`, `npm install`, then `node server.js`.
4. In Terminal 2 (Client): `cd client`, `npm install`, then `npm start`.
